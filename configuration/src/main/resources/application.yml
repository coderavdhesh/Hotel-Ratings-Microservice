server:
  port: 8095



spring:
  application:
    name: Config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/coderavdhesh/microservice-config-server
          clone-on-start: true